# Begin by ignoring everything (just in case).
*

# Include application files.
!app/

# Include build files.
!pyproject.toml
!poetry.lock
!entrypoint.sh

# Include Elastic Beanstalk configuration files.
!.ebextensions
